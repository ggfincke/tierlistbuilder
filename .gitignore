# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
desktop.ini
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# typescript build cache — produced by `tsc -b`, lives at the path each
# tsconfig points to via tsBuildInfoFile (we use node_modules/.tmp/), but
# orphan files outside that path should never be tracked
*.tsbuildinfo

# local AI agent metadata
.agents
.claude
skills-lock.json

# Example assets (local dev only)
/examples

# Generated screenshots (local dev only)
/screenshots

# Playwright output
/test-results
/playwright-report
/.playwright
/playwright/.cache

# Vitest coverage output
/coverage
/.nyc_output

# vite-plugin-pwa dev build output
dev-dist

# ESLint cache (when run w/ --cache)
.eslintcache
