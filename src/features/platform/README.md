# src/features/platform

Reserved namespace for platform capabilities — auth, sync, media, short links, notifications, moderation.

Populated incrementally across Phase 7. See `dev-docs/directory-restructure-proposal.mdx` for the long-form plan & boundary rules.

Current contents:

- `backend/` — shared `ConvexReactClient` singleton & backend boundary docs
